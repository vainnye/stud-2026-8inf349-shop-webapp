[project]
name = "stud-2026-8inf349-shop-webapp"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "peewee>=4.0.0",
    "pytest>=9.0.2",
    "pytest-flask>=1.3.0",
]

[tool.uv.workspace]
members = [
    "temp",
]
